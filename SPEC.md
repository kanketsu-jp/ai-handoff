# AI Handoff — 仕様

## 0. 前提 — 2 つの LLM は記憶を共有しない

いま、人と人のやり取りはこうなっています。

```
私 → 自分の LLM に相談 → 文章を作る → 送る
                                    → 相手が受け取る → 相手の LLM に相談 → 文章を作る → 送る → …
```

**LLM が両端にいるのに、真ん中で人間が文章を運んでいます。** これは過渡期の形です。

ただし、いまの形には**捨ててはいけない利点**があります。
それぞれの LLM は、**その環境にしかないもの**を持っています。

```
🟢 越境させないもの（各自の環境に留まる）
   ・秘密情報（トークン・鍵・顧客データ）
   ・会社固有の知識・過去のやり取り・社内ルール
   ・その人がインストールした Skill / MCP / ツール
```

だから「API で直結」ではなく、**ファイルを 1 枚渡す**形にしています。
**越境するのはファイルだけ**で、環境は混ざりません。

そして、いまの形の**捨てるべき部分**はこれです。

```
🚨 文脈が共通化されない … 相手の LLM は、こちらのシステムを何も知らない
🚨 往復が増える ………… 人間が両端で「これはどういう意味か」を通訳している
🚨 コピペが挟まる ……… LINE の文字を人が LLM に貼り直している。ここは AI ネイティブではない
```

**このプロトコルは、その 3 つだけを解きます。**
ファイルに文脈を明示的に載せ、往復を 1 回に減らし、コピペの手数を消します。

---

## 1. 成果物

このプロトコルには 2 つの成果物しかありません。

| ファイル | 誰が書くか | 誰が読むか |
|---|---|---|
| `REQUEST.md` | 依頼側の人間（＋その LLM） | **相手の LLM** |
| `BRIEF.md` | 相手の LLM | **依頼側の LLM** |

**どちらも人間が読める必要はありますが、人間のために最適化しません。**
最適化するのは「LLM がそのまま実行できること」です。

---

## 2. 言語 — 本文は英語、会話は利用者の言語

**`REQUEST.md` と `BRIEF.md` の本文は英語で書きます。**

読み手は LLM であって人間ではありません。同じ内容を日本語で書くと
**トークンがおよそ 1.5〜2 倍**になります。往復のたびに効いてくるので、本文は英語に固定します。

ただし**会話は別**です。相手の人間が日本語で話す人なら、LLM は日本語で話しかける必要があります。
これを**本文のどこかに書くと読み飛ばされる**ので、**先頭のメタ情報**に置きます。

### 2-1. 先頭のメタ情報（フロントマター）

`REQUEST.md` の 1 行目から書きます。

```yaml
---
protocol: ai-handoff/1
lang: ja
from: kazuma-horiike (KANKETSU Inc.)
people:
  - id: r-tanaka
    to: primary
    role: decision-maker
    tech: non-eng
    knows: html,css,llm-assisted-pages
    gap: next.js,headless-cms,deploy
    terms: explain-1line-on-first-use
    decides: content,copy,photos
    defers: pricing->manager
---
```

| キー | 意味 |
|---|---|
| `protocol` | プロトコルとその版。`ai-handoff/1` 固定 |
| `lang` | 🚨 **利用者と会話するときの言語**（BCP 47。`ja` / `en` / `ko` …） |
| `from` | 依頼元。誰からの依頼として話せばよいか |
| `people` | 🚨 **宛先**。1 人でも配列で書く（§2-3） |

**`lang` は会話の言語であって、文書の言語ではありません。**
文書は常に英語です。

### 2-2. 🚨 `people` — 宛先と、話し方を決める情報

**人物に関する情報は、本文に散文で書かず、ここに機械向けの短い記法でまとめます。**

理由は 2 つあります。

```
① 本文に「この人は〜な人です」と書くと、渡した相手がそれを読む
   🚨 ファイルは相手の手元に残るので、書いた側の意図と関係なく人物評として読まれる
② 散文は長い。同じ情報が 1/3 のトークンで書ける
```

| キー | 何を書くか | 例 |
|---|---|---|
| `id` | 短い識別子（`BRIEF.md` で誰の発言かを指すのに使う） | `r-tanaka` |
| `to` | `primary`（この人に聞く）/ `cc`（同席・確認先） | `primary` |
| `role` | その案件での立場 | `decision-maker` |
| `tech` | 技術的な前提 | `non-eng` / `eng` / `designer` |
| `knows` | 説明が要らない領域（カンマ区切り） | `html,css` |
| `gap` | 経験の外にある領域。**ここは丁寧に説明する** | `next.js,headless-cms` |
| `terms` | 用語の扱い | `explain-1line-on-first-use` |
| `decides` | その人が単独で決められること | `content,copy,photos` |
| `defers` | その人が他へ回すこと | `pricing->manager` |

**書いてよいのは、LLM の振る舞いが実際に変わる項目だけです。**
振る舞いが変わらない情報は、ただの人物評なので書きません。

### 2-3. 🚨 `people` は読み上げない

**LLM への入力であって、会話に出す材料ではありません。**

```
❌ 「カズマさんから、専門用語は 1 行で説明するように言われています」
✅ 黙って 1 行の説明を付ける
```

相手に読み上げると、**短く書いた意味が消えます**（散文で書いたのと同じことになる）。
語彙と説明の深さを決めるためだけに使います。

🚨 **短く書いても、隠したことにはなりません。** ファイルは相手の手元にあり、開けば読めます。
**読まれても問題にならない書き方をする**のが本体で、短さはその手段です。

### 2-4. 宛先が複数のとき

`people` が 2 人以上なら、**LLM は自分のオーナーがどれかを最初に確かめます。**

```
・自分のオーナーが primary … そのまま進める
・自分のオーナーが cc ……… primary の人に確認が要る項目を、BRIEF.md に分けて書く
・どれにも該当しない ……… 🚨 進めずに確認する（別人に渡っている可能性がある）
```

`BRIEF.md` では、**発言ごとに `id` で誰の言葉かを示します。**

### 2-5. 何を英語で書き、何を書かないか

| | 言語 |
|---|---|
| `REQUEST.md` の本文 | **英語** |
| `BRIEF.md` の本文・見出し・JSON のキー | **英語** |
| 🚨 **利用者が言った言葉の引用** | **利用者の言語のまま**（要約も翻訳もしない） |
| 🚨 固有名詞（店名・人名・URL・商品名） | **元の表記のまま**（訳すと照合できなくなる） |
| 利用者との会話 | `lang` の言語 |

引用を訳してしまうと、依頼側が「本人は本当にそう言ったのか」を確かめられません。
**引用は証拠なので、加工しません。**

---

## 3. 🚨 相手の LLM は、まず自分の環境を自己判定する

**同じ「Claude」でも、環境によってできることが違います。**
ターミナルで動いているのか、デスクトップアプリなのか、スマホなのかで、
ファイルを書けるかどうかも、外部ツールにつなげるかどうかも変わります。

だから **`REQUEST.md` は手段を指定しません。目的だけを書き、手段は相手の LLM が選びます。**

### 3-1. 判定する 3 つの能力

| 能力 | できるとき | 🚨 できないときの代わり |
|---|---|---|
| **ファイルの書き出し** | `BRIEF.md` をディスクに保存 | ダウンロードできる形で出す → それも無ければ **チャットに 1 つのコードブロックで全文出す** |
| **まとめて答えてもらう UI** | HTML フォーム・選択 UI | **1 問ずつ会話で聞く**／JSON の雛形を出して埋めてもらう |
| **外部ツール接続（MCP）** | 管理画面などに直接つなぐ | **つながずに聞き取りだけで完結**（データ入力は依頼側がやる） |

🚨 **判定は「実際にやってみて」決めます。** できると宣言してから失敗するのが最悪です。

### 3-2. 環境ごとの目安

| 環境 | ファイル | まとめて回答 | MCP の入れ方 |
|---|---|---|---|
| **Claude Code**（ターミナル / IDE） | ディスクに直接書ける | HTML を書いて開いてもらう | `~/.claude.json` を編集 |
| **Claude デスクトップ / Web** | ダウンロードできる形で出す | Artifact で出せる | 設定 → コネクタ → カスタムコネクタ |
| **Claude モバイル** | 🚨 チャットに全文を出す | 🚨 1 問ずつ | 🚨 入れられない |
| **他社の LLM** | 環境による | 環境による | 🚨 たいてい入れられない |

🚨 **この表は目安であって、根拠ではありません。** 各サービスの機能は変わります。
**表を信じずに、その場で試してください。**

### 3-3. できなかったことは、必ず `BRIEF.md` に書く

**黙って別の手段に切り替えないでください。**
依頼側は「なぜ想定と違う形で返ってきたのか」が分からず、確認の往復が増えます。
**往復を減らすのがこのプロトコルの目的**なので、黙る代替はプロトコル違反です。

`BRIEF.md` の冒頭に、**環境・できなかったこと・その理由・代わりにやったこと**を書きます。

```markdown
## 0. Environment

- Runtime: Claude mobile app
- 🚨 Could not write a file (no filesystem access) -> this brief is pasted into chat as a single block
- 🚨 Could not connect to the admin panel (MCP unavailable on mobile) -> no data was entered; see §3
```

---

## 4. `REQUEST.md` の規則

### 4-1. 冒頭で、読み手が LLM であることを宣言する

フロントマターの直後から、相手の人間ではなく**相手の LLM に話しかけます。**

```markdown
> **This file is a request addressed to you (the LLM), not to your owner.**
> Interview your owner, then produce `BRIEF.md`.
> 🚨 Read the whole file first. Then start a conversation, one topic at a time.
```

### 4-2. 🚨 いっぺんに全部聞かせない

**これがこのプロトコルの本体です。** 質問表を LLM 経由で丸投げするだけなら、何も改善しません。

```
✅ 1 項目ずつ、会話で進める
✅ 相手が「まとめて答えたい」と言ったときだけ、まとめる
🚨 手段は指定しない（§3。相手の環境で出せるものが違う）
```

### 4-3. 前提情報を惜しまず書く

相手の LLM は、あなたのシステムを何も知りません。**知らないまま質問を作ると、
的外れなヒアリングになります。**

```
✅ 何を作っているか（URL・ページ構成・下層ページや動的パスの一覧）
✅ いま何が「ダミー」で何が「正」か
✅ 変更の反映しやすさ（どこは簡単で、どこは設計に触るか）
✅ 用語（相手が知らない略語は必ず 1 行で説明する）
```

### 4-4. 🚨 相手の人間を「評価」しない

書いてよいのは**何を説明すべきか**であって、**その人がどういう人か**ではありません。

```
❌ "Your owner is a non-engineer who understands some HTML."
✅ "Your owner decides this project but does not implement it.
    Explain any technical term in one line the first time it appears."
```

🚨 **`REQUEST.md` は相手の手元に残ります。**
人物評は、書いた側の意図と関係なく、そのまま読まれます。

### 4-5. 🚨 `REQUEST.md` は返ってこない

渡したら回収できません。**第三者の個人情報・社内の判断・他案件の話を書かないこと。**
渡す前に、**自分以外の目で 1 回読む**（別の LLM でも人でもよい）。

### 4-6. 秘密情報は本文に書かない

トークンや鍵は**別の経路**で渡し、`REQUEST.md` には「別途受け取ってください」とだけ書きます。
相手の LLM には、**設定手順を相手に案内させます**（値そのものを LLM に扱わせない）。

🚨 **渡すトークンの権限を、渡す相手の権限に合わせます。**
管理者トークンを渡すと、`REQUEST.md` に書いた「ここだけ触ってください」は意味を持ちません。

---

## 5. `BRIEF.md` の規則

### 5-1. 🚨 推測を書かない。分からないものは `unconfirmed` と書く

**このプロトコルで最も重要な規則です。**

```markdown
❌ "They probably want updates about twice a week."
✅ "update_frequency: unconfirmed (asked; owner has not decided)"
```

埋まっていない項目が残ったまま返して構いません。**空欄より、埋められた推測のほうが害があります。**

### 5-2. 構造化できるものは JSON のコードブロックで返す

散文で返すと、依頼側が読み直して構造化し直すことになります。それでは往復が減りません。

````markdown
```json
{
  "stores": [
    { "name": "◯◯店", "brand": "be", "address": "unconfirmed", "open_date": "2026-04" }
  ]
}
```
````

**スキーマは `REQUEST.md` 側が提示します。** 相手の LLM が勝手に決めません。
🚨 **キーは英語、値のうち固有名詞は元の表記のまま**（§2-5）。

### 5-3. 誰が決めたのかを書く

```markdown
| Item | Answer | Decided by | Status |
|---|---|---|---|
| … | … | owner | settled |
| … | — | **needs confirmation (their manager)** | pending |
```

依頼側は「これは本当に決まったのか」を毎回聞き返さずに済みます。

### 5-4. 途中で返してよい

```markdown
## Progress
- Answered: 3 / 6 topics
- 🚨 Not started: sample articles, design feedback
- This brief is **partial**. The rest will follow in a later brief.
```

### 5-5. 原文を残す

要約すると、依頼側が判断できなくなります。**相手が言った言葉は `>` で引用して残します。**
🚨 **引用は翻訳しません**（§2-5）。

### 5-6. 🚨 環境と、できなかったことを書く

§3-3 のとおり、冒頭に `## 0. Environment` を置きます。**できたときも書きます**
（「書いていない」と「できた」が同じ見た目になるため）。

---

## 6. 守らなくてよいこと

- **フォーマットの完全一致**。項目が足りていて、推測が混ざっていなければ通用します
- **1 回で終わらせること**。往復して構いません
- **全部埋めること**。§5-4 のとおり途中で返せます

---

## 7. アンチパターン

- ❌ `REQUEST.md` に質問を 20 個並べて「これに答えて」と書く（**質問表の LLM 経由送付**にしかならない）
- ❌ 相手の LLM に「まとめて聞け」と指示する（§4-2 の逆）
- ❌ 🚨 **手段を指定する**（「HTML を作れ」「ファイルに書け」）。環境によっては実行できない（§3）
- ❌ 🚨 **できなかったことを黙って代替して返す**（§3-3。依頼側が理由を追う往復が生まれる）
- ❌ 🚨 **本文を日本語で書く**（§2。読み手は LLM。トークンが 1.5〜2 倍になる）
- ❌ 🚨 **利用者の発言や固有名詞を英語に訳す**（§2-5。証拠と照合先が壊れる）
- ❌ 🚨 **相手の人間の技術レベルや人物像を書く**（§4-4。ファイルは相手の手元に残る）
- ❌ `BRIEF.md` に推測を書く（§5-1）
- ❌ 構造化できるデータを散文で返す（§5-2）
- ❌ `REQUEST.md` に秘密情報・第三者の話を書く（§4-5 / §4-6）
- ❌ 権限の広すぎるトークンを渡す（§4-6）
- ❌ 決めた人を書かない（§5-3。依頼側が確認のために往復することになる）
